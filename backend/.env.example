# Claude Agent SDK Authentication
# Use ONE of the following options:

# Option 1: Anthropic Console API Key (pay-as-you-go billing)
# Get from: https://console.anthropic.com/settings/keys
# Format: sk-ant-api03-...
# ANTHROPIC_API_KEY=your-api-key-here

# Option 2: Claude Pro/Max Subscription OAuth Token
# Get by running: claude setup-token
# Format: sk-ant-oat01-...
# CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token-here

# Option 3: If you've already authenticated Claude Code CLI (by running 'claude' in terminal),
# the SDK uses that authentication automatically and you don't need to set either variable

# Debug mode - set to any value to enable debug logging
# DEBUG=1

# API Key Authentication (optional)
# If set, requests to protected endpoints must include X-API-Key header
# If not set or empty, authentication is disabled (for local development)
# Protected endpoints: /api/generate, /api/regenerate-card, /api/export
# Unprotected endpoints: /health, /api/config
# API_KEY=your-secure-api-key-here

# CORS Configuration (optional)
# Comma-separated list of allowed origins, or "*" for all origins (development only)
# Default: http://localhost:3000
# CORS_ORIGINS=http://localhost:3000,https://myapp.com

# Claude Model Configuration (optional)
# Model to use for card generation
# Default: claude-sonnet-4-5-20250929
# CLAUDE_MODEL=claude-sonnet-4-5-20250929

# AnkiConnect Configuration (optional)
# Timeout in seconds for AnkiConnect requests
# Default: 30.0
# ANKI_CONNECT_TIMEOUT=30.0

# Configuration Cache TTL (optional)
# How long to cache the anki_config.json in seconds
# Default: 300 (5 minutes)
# CONFIG_TTL=300

# Migration Configuration (optional)
# These settings are for migrating notes from one Anki note type to another
# Note type to migrate FROM (the old format)
# MIGRATE_OLD_NOTE_TYPE=Philippe's Japanese v3
# Note type to migrate TO (the new format)
# MIGRATE_NEW_NOTE_TYPE=Japanese Vocabulary (Agent)
# Field mapping as JSON object (old field names -> new field names)
# MIGRATE_FIELD_MAPPING={"Kana":"Hiragana/Katakana","Romaji":"Romaji","Kanji":"Kanji","English":"English","Nederlands":"Dutch","Example":"Example sentence hiragana/katakana","Example Kanji":"Example sentence kanji","Example translation":"Example sentence translation","Extra":"Extra notes","Sound":"Sound","Sound Example":"Sound example"}
