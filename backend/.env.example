# Claude Agent SDK Authentication
# Use ONE of the following options:

# Option 1: Anthropic Console API Key (pay-as-you-go billing)
# Get from: https://console.anthropic.com/settings/keys
# Format: sk-ant-api03-...
# ANTHROPIC_API_KEY=your-api-key-here

# Option 2: Claude Pro/Max Subscription OAuth Token
# Get by running: claude setup-token
# Format: sk-ant-oat01-...
# CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token-here

# Option 3: If you've already authenticated Claude Code CLI (by running 'claude' in terminal),
# the SDK uses that authentication automatically and you don't need to set either variable

# Debug mode - set to any value to enable debug logging
# DEBUG=1

# API Key Authentication (optional)
# If set, requests to protected endpoints must include X-API-Key header
# If not set or empty, authentication is disabled (for local development)
# Protected endpoints: /api/generate, /api/regenerate-card, /api/export
# Unprotected endpoints: /health, /api/config
# API_KEY=your-secure-api-key-here
